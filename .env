#############################
#  Authentication / Secrets #
#############################

# OpenAI (GPT-4o, GPT-4-Turbo, etc.)
RAG_OPENAI_KEY=sk-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# --- optional --- uncomment if you’ll call Google Med-PaLM-2 or Gemini Pro
# RAG_VERTEX_PROJECT=my-gcp-project-id
# (if you need a custom service-account file, just set GOOGLE_APPLICATION_CREDENTIALS
#  in your shell or Dockerfile; the code picks it up automatically.)

#############################
#  Runtime Tweaks (optional) #
#############################

# Log level for all agents: DEBUG | INFO | WARNING | ERROR
# RAG_LOG_LEVEL=INFO

# Max tokens for each summary draft (default 4096)
# RAG_MAX_TOKENS_SUMMARY=4096

# Override the critic weight triple (must still sum to 1.0)
# RAG_CRITIC_WEIGHTS=0.6,0.3,0.1

# Directory where Jinja prompt templates live (defaults to prompts/)
# RAG_PROMPT_DIR=prompts/
